# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (API keys)
.env
.env.*

# Resume cache for `npm run generate-scores` (intermediate only —
# the committed artifact is src/data/ai_scores.json)
data/ai_scores_cache.json

# Raw BLS API response cache for `node scripts/refresh_oes_data.mjs`
# (resume/quota aid only — the committed artifacts are src/data/geo_real.json
# and src/data/national_employment.json)
data/.oes_api_cache.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
