export const Z = {
    base: 10,
    /** Above drei Html job labels (zIndexRange ~100) so the year slider stays on top. */
    timeBar: 110,
    header: 20,
    sidebar: 150,
    detailPanel: 200,
    modal: 250,
    modalTop: 300,
    toast: 9000,
    errorBoundary: 9999,
} as const;
